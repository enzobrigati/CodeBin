{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}
    {% if app.user %}
    <paste-form/>
    {% else %}
        <div class="container mt-4">
            <div class="alert alert-info">
                Vous devez être connecté pour poster un paste. <a href="{{ path('app_login') }}" class="text-primary">Rendez-vous ici</a>
            </div>
        </div>
    {% endif %}
{% endblock %}
